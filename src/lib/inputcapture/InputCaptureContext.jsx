import { createContext } from 'react';

export const InputCaptureContext = createContext(
  /** @type {ReturnType<import('./UseInputCaptureContextValue').useInputCaptureContextValue>|null} */ (
    null
  ),
);
